# Claude Code

The instructions for this repo live in [AGENTS.md](AGENTS.md). Read it before running anything: it covers what to
ask the user first, the safety rules (never root, never print secrets), the one-command path (`./launch.sh`), the
two steps only a human can do, and what to do when something breaks.
